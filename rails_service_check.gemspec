Gem::Specification.new do |s|
  s.name = "rails_service_check"
  s.summary = "Adds a simple service check route and controller to your rails app"
  s.author = "Mark Woods"
  s.files = `git ls-files`.split("\n")
  s.version = "0.0.1"
end
